namespace MobilePhone
{
    using System;
    using System.Linq;

    public class GSMTest
    {
        /// static void Main()
        /// {
       ///    GSM firstGSM = new GSM("sony experia", "Sony", 240, "me");
       ///    GSM secondGSM = new GSM("Nokia Asha", "Nokia", 410, "You", new Battery("China", "200", "10"), new Display(3.2, 16));
       ///    GSM[] GSMs = new GSM[] { firstGSM, secondGSM, GSM.IPnone4S };
        ///    foreach (var phone in GSMs)
        ///    {
        ///        Console.WriteLine(phone);
        ///    }
        /// }
    }
}
